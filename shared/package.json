{
  "name": "@ispyai/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types between the iSpyAI backend and dashboard.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
